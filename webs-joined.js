/* Level 14 — Webs Joined */
(function(){'use strict';const $=id=>document.getElementById(id);const esc=v=>String(v??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));function session(){return window.__sadeeqConsoleSession?.()||''}async function load(){const root=$('page-webs');if(!root)return;root.innerHTML='<div class="module-hero"><div><span class="section-label">CONNECTED WEBS</span><h2>Webs Joined</h2><p>Duk websites da suke amfani da bots ɗinka suna bayyana a nan.</p></div><div class="module-badge">LIVE CONTROL</div></div><section class="form-panel"><div class="panel-heading"><div><span class="section-label">WEBSITES</span><h3>Registered Websites</h3></div><button id="wj-refresh" class="secondary-btn">↻ Refresh</button></div><div id="wj-list" class="empty-state">Ana loda...</div></section>';const r=await window.client?.rpc('owner_list_webs_joined',{p_session:session()});const e=$('wj-list');if(!e)return;if(r?.error||!r?.data?.ok){e.textContent='Unable to load websites.';return}const rows=r.data.websites||[];e.innerHTML=rows.length?rows.map(x=>`<article class="joined-card"><div><strong>${esc(x.website_name)}</strong><span>Website ID: ${esc(x.website_id)}</span><span>Bot: ${esc(x.bot_id)}</span><span>Ticket: ${esc(x.registered_ticket)}</span><span>Joined: ${new Date(x.created_at).toLocaleString()}</span></div><div class="joined-meta"><b class="status-${esc(x.status)}">${esc(x.status).toUpperCase()}</b><div class="joined-actions"><button data-action="active" data-id="${x.id}" class="secondary-btn">Active</button><button data-action="suspended" data-id="${x.id}" class="secondary-btn">Suspend</button><button data-action="removed" data-id="${x.id}" class="danger-btn">Remove</button><button data-action="cancel" data-id="${x.id}" class="secondary-btn">Cancel</button></div></div></article>`).join(''):'No websites have joined yet.';document.querySelectorAll('#wj-list [data-action]').forEach(b=>b.onclick=async()=>{if(b.dataset.action==='cancel'){load();return}b.disabled=true;const q=await window.client.rpc('owner_set_web_status',{p_session:session(),p_id:b.dataset.id,p_status:b.dataset.action});if(q.error||!q.data?.ok)alert(q.data?.error||'Action failed');load()});$('wj-refresh').onclick=load}window.showWebsJoined=load})();