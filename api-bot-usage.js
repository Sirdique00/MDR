/* Level 14 — API Bot Usage */
(function(){'use strict';const $=id=>document.getElementById(id);const esc=v=>String(v??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));function session(){return window.__sadeeqConsoleSession?.()||''}async function load(){const root=$('page-usage');if(!root)return;root.className='page active-page';root.innerHTML='<div class="module-hero"><div><span class="section-label">BOT ANALYTICS</span><h2>API Bot Usage</h2><p>Usage na kowanne website da bot a wuri guda.</p></div><div class="module-badge">LIVE METRICS</div></div><section class="form-panel"><div class="panel-heading"><div><span class="section-label">USAGE</span><h3>Website Activity</h3></div><button id="abu-refresh" class="secondary-btn" type="button">↻ Refresh</button></div><div id="abu-list" class="empty-state">Ana loda...</div></section>';const r=await window.client?.rpc('owner_list_api_usage',{p_session:session()});const e=$('abu-list');if(!e)return;if(r?.error||!r?.data?.ok){e.textContent='Ba a iya loda usage ba. Gwada Refresh.';return}const rows=Array.isArray(r.data.usage)?r.data.usage:[];e.innerHTML=rows.length?'<div class="usage-grid">'+rows.map(x=>`<article class="usage-card"><strong>${esc(x.website_name)}</strong><span>Bot: ${esc(x.bot_id)}</span><div><b>Requests today</b><em>${Number(x.requests_today||0).toLocaleString()}</em></div><div><b>Total requests</b><em>${Number(x.requests_total||0).toLocaleString()}</em></div><div><b>Responses today</b><em>${Number(x.responses_today||0).toLocaleString()}</em></div><div><b>Total responses</b><em>${Number(x.responses_total||0).toLocaleString()}</em></div><small>Last activity: ${x.last_activity?new Date(x.last_activity).toLocaleString():'—'} • ${esc(x.status)}</small></article>`).join('')+'</div>':'Babu usage da aka rubuta tukuna.';$('abu-refresh').onclick=load}window.showApiBotUsage=load})();