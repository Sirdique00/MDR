/* Sadeeq AI Hub • Bot Settings — FINAL embed contract patch */
/* This file is intentionally kept compatible with the existing settings renderer. */
(function(){
'use strict';
/* The authoritative embed format is bot.html?bot_id=<BOT_ID>. Secret ID is NEVER embedded. */
window.SADEEQ_EMBED_CONTRACT={version:'final-1',secretInIframe:false,requiresOneTimeWebsiteRegistration:true};
})();
